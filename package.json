{
  "name": "taskorbit",
  "private": true,
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "guard:docs": "node scripts/enforce-doc-updates.mjs",
    "lint": "yarn guard:docs && turbo lint",
    "typecheck": "turbo typecheck",
    "format": "turbo format"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  }
}
