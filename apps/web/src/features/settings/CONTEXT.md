# Settings Context

Settings should remain low-friction and trustworthy:

- preference changes must persist clearly
- failures must be visible and recoverable
- status labels should be explicit, not ambiguous
