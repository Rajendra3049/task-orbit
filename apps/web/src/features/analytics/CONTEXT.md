# Analytics Context

Analytics is decision support, not raw data dumping.

For every metric:

- expose definition in plain language
- show enough context to interpret confidence
- avoid misleading precision
