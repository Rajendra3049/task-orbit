# Shared Module

Cross-feature runtime pieces:

- `hooks` - app-wide hooks (auth/user)
- `store` - UI/global state (mode/sidebar)
- `providers` - root providers
- `constants` - navigation and config constants
- `lib` - framework-neutral helpers
